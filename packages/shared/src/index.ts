// M0: empty placeholder. The Money value object, date/timezone helpers, and the
// shared Result/error types land here starting M2 (docs/14-MILESTONES.md) — not
// before, since Money in particular needs the FIFO/weighted-average decision
// (docs/01-OPEN-DECISIONS.md A3) settled first for anything beyond the type itself.
export const SHARED_PACKAGE_PLACEHOLDER = true;
