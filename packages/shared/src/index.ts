// Barrel export for everything the desktop app and the API share:
// Zod validation schemas, DTOs/types generated from them, and i18n keys.
// Populated starting with the Foundation phase (auth/users/roles).

export const SHARED_PACKAGE_READY = true;
