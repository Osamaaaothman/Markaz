{
  "name": "@erp/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "prisma": {
    "seed": "node --env-file=../../.env --experimental-strip-types prisma/seed.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "node --env-file=../../.env scripts/with-migrate-url.mjs prisma migrate dev && node --env-file=../../.env scripts/sync-app-role-password.mjs",
    "migrate:deploy": "node --env-file=../../.env scripts/with-migrate-url.mjs prisma migrate deploy && node --env-file=../../.env scripts/sync-app-role-password.mjs",
    "seed": "node --env-file=../../.env --experimental-strip-types prisma/seed.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@prisma/client": "^6.2.1",
    "pg": "^8.13.1",
    "uuidv7": "^1.0.2"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  }
}
