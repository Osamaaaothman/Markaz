PORT=3000
DATABASE_URL="postgresql://erp:erp@localhost:5432/erp?schema=public"
JWT_SECRET="change-me"

# Used by `pnpm prisma:seed` to create the initial admin user. Change the
# password before running against anything other than local dev.
ADMIN_SEED_EMAIL="admin@markaz.local"
ADMIN_SEED_PASSWORD="changeme123"
